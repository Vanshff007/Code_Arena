// Real content (Start Battle, Join Battle, Leaderboard, etc.) and the
// ProtectedRoute wrapper around this page both land in Step 5.
function Dashboard() {
  return (
    <main className="mx-auto max-w-md px-6 py-24 text-center">
      <h1 className="text-2xl font-semibold text-gray-900 dark:text-gray-100">Dashboard</h1>
      <p className="mt-2 text-gray-600 dark:text-gray-400">Coming in Step 5.</p>
    </main>
  );
}

export default Dashboard;
