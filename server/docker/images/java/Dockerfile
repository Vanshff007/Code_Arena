# Submitted code must declare `public class Main` - javac/java require the
# public class name to match the filename (Main.java).
FROM eclipse-temurin:21-jdk-jammy
RUN useradd -m -u 1000 sandbox
USER sandbox
WORKDIR /box
